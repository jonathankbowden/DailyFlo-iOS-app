# DailyFLO design prompt log

Kept by the `design-level-up` skill. Log build prompts and creative directions here, including the ones that did not work and why. Re-test failed ones when a newer model arrives.

House constraints every prompt must honor: soft neutrals and nature tones only (no pinks or purples), no body imagery, SwiftUI-native controls, tokens from `DesignSystem.swift`.

## Prompts

_(none yet)_
